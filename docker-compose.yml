services:

  nextjs:
    container_name: admin-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3006:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped

